fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.6.0
numpy==1.26.4
# ifcopenshell is optional — fallback parser works without it
# for full STEP + BVH: pip install ifcopenshell  or  mamba install -c conda-forge ifcopenshell
# ifcopenshell==0.8.5
# dev (optional)
# pytest==8.3.0
# httpx==0.27.0
