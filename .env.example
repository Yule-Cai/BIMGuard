# Optional LLM for agent explanations (tool-router still works without)
OPENAI_API_KEY=sk-xxxx
# or compatible endpoint
OPENAI_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# Backend
PORT=8000
